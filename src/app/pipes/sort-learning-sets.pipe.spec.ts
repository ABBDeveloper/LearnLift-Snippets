import { SortLearningSetsPipe } from './sort-learning-sets.pipe';

describe('SortLearningSetsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortLearningSetsPipe();
    expect(pipe).toBeTruthy();
  });
});
